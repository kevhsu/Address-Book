<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @group.name %>
</p>

<p>
  <b>Description:</b>
  <%= @group.description %>
</p>

<p>
    <b>Contacts:</b>
    <% @group.contacts.each do |contact| %>
        <li>
            <%= link_to "#{contact.firstname} #{contact.lastname}", contact %>
        </li>

    <% end %>
</p>
<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
